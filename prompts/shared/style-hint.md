样式：
- 可在 <style> 中定义 class，也可用 inline style
- 编辑时优先改 #doc 内结构与 inline style；需要时可增改 head <style>
- 常用：font-size, color, font-weight, line-height, letter-spacing, margin, padding,
  background / background-color, text-align, border, border-radius, width, max-width, display, gap, flex, grid
